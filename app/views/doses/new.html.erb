<%= simple_form_for [ @cocktail, @dose ] do |f| %>
  <%= f.input :description, label: "Amount", placeholder: "1.5 ml, 3 pieces, ..."%>
  <%= f.association :ingredient%>
  <%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
